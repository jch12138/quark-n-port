# Release images allow exactly one interactive root/root login for first-login
# provisioning.  quark-first-login replaces the root password and rewrites
# this file with PermitRootLogin no after creating the normal user.
PermitRootLogin yes
PasswordAuthentication yes
